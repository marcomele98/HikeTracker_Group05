version: "3.8"
services:
  backend:
    build: ./server
    ports:
      - '3001:3001'
  frontend:
    build: ./client
    ports: 
      - '3000:3000'
    stdin_open: true
    tty: true
    depends_on: 
      - backend